import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonWrapper: {
    borderRadius: 8,
    overflow: 'hidden',
  },
  button: {
    paddingHorizontal: 32,
    paddingVertical: 12,
    borderRadius: 6,
    overflow: 'hidden',
    margin: 2,
  },
});
