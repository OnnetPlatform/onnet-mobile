import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonWrapper: {
    borderRadius: 8,
    overflow: 'hidden',
  },
  button: {
    paddingHorizontal: 16,
    paddingVertical: 8,
    borderRadius: 6,
    overflow: 'hidden',
    margin: 2,
  },
});
