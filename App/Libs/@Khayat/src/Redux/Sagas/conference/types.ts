export enum ConferenceEvents {
  CONNECTED = 'connected',
  DISCONNECTED = 'disconnect',
  OFFER = 'offer',
  ANSWER = 'answer',
  CANDIDATE = 'candidate',
  JOINED_USERS = 'joined_users',
  ESTABLISH_OFFER = 'establish_offer',
  JOIN = 'join',
}
