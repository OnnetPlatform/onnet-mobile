import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 300,
    backgroundColor: 'red',
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 4,
  },
});
